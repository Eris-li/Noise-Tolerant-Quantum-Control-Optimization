# rydcalc slides

This directory contains a Chinese Beamer slide deck introducing `rydcalc` and the implementation details of `total_decay`.

Build with XeLaTeX:

```bash
xelatex main.tex
```

The main source is `main.tex`.
